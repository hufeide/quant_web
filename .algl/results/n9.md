# n9 结果 — 装配与一致性终检

把 7 个补齐批次（n2–n8）的全部注册表文件装配进网页与文档，并跑通端到端闸门。

## 装配动作
1. **接入网页**：在 `index.html` 的 `F14-collateral.js` 之后补挂 `G1-ai-decision.js` / `G2-core-assets.js` / `G3-quant-portfolio.js` / `G4-specialized.js` / `G5-ops-market.js` / `G6-misc.js` / `G7-institutions.js`（此前仅引到 F14，e2e 第 1 项会报缺失）。
2. **重新生成文档**：运行 `tools/gen-docs.mjs`，`FUNCTIONS.md` 刷新为 **24,538 行 / 621.3 KB / 4,861 项功能**，与注册表同源、覆盖全部 63 模块。
3. **修正 README 头部数字**：`**4861 项功能**`、`613 个功能面板`、`4248 项子功能`、`63 个业务模块`，并同步「16 大分组、63 个模块」「613 个面板」等计数，满足 e2e 精确匹配。
4. **修复深链路由**（真实 bug）：`app.js` 特征路由正则 `/#\/f\/([A-Z]+-\d+)/i` 不允许 ID 前缀含数字，导致 `G2/G3/G4` 类 ID（如 `G4APM-01`）的 `#/f/<id>` 深链抽屉打不开（README 宣称的「`#/f/FAC-08` 直接打开功能说明」对含数字前缀 ID 失效）。放宽为 `([A-Z0-9]+-\d+)`，深链对全部 ID 生效。

## 终检结果（四道闸门全绿）
| 闸门 | 命令 | 结果 |
|---|---|---|
| 结构 | `node tools/check.mjs` | ✅ 全部检查通过 |
| 批量 | `node tools/batch-check.mjs --file G2/G3/G4 ...` | ✅ 各 ≥10 面板 / ≥40 子功能 |
| 端到端 | `node tools/e2e-check.mjs` | ✅ 62 注册表文件 / 63 模块 / 613 面板 / 4248 子功能 / 4861 功能 |
| DOM 渲染 | `node tools/dom-check.mjs` | ✅ 63 模块 + 全部特征抽屉渲染正常，无 undefined/NaN |

## 最终规模
- 业务模块：**63**
- 功能面板：**613**
- 子功能：**4,248**
- 功能总数：**4,861**
- AI/创新功能：290（面板级 + AI 标记子功能）

ALGL 循环 n2–n9 全部完成。
